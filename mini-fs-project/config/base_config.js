/*
  基础信息配置文件
 */

// 全局错误提示
let errMsg = '网络错误，请稍后重试~'

// 飞书小程序版本历史：大版本.小版本.优化版本或极少功能的小版本(每次发版，向后追加发版新版本号)
const VERSION = [
    '1.0.0',
]

const appid = 534534543 // 小程序渠道id
const secret = 'fawefawfeawfewfawefewf' // 小程序的appsecret


export {
    VERSION,
    errMsg,
    appid,
    secret,
}
